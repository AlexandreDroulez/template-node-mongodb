let mongoose,configs;_ff7‍.x([["connect",()=>connect],["getConnect",()=>getConnect],["close",()=>close]]);_ff7‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_ff7‍.w("../configs",[["default",["configs"],function(v){configs=v}]]);/* eslint no-console: 0 */





/**
 * Connect mongoose to database
 * @returns {void}
 */
       async function connect() {
  await mongoose.connect(configs.services.mongodb.uri, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
    useCreateIndex: true,
  });
  console.log('Mongo Connected');
}

       const getConnect = () => mongoose.connection;

/**
 * Close the mongoose connection to database
 * @returns {void}
 */
       async function close() {
  await mongoose.disconnect();
  console.log('Mongo Disconnected');
}
