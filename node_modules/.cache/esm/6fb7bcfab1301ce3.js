_b13‍.x([["encryptionService",()=>encryptionService]]);const Bcrypt = require("bcryptjs");

       const encryptionService = {

    hashPassword  (value) {
        return Bcrypt.hashSync(value, 10);
    },

}
